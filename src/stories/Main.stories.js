import Main from "../components/main";

const Template = () => <Main />;

export default {
  title: "Example/Main",
  component: Main,
};

// export const Template;
export const Primary = Template.bind({});
Primary.args = {};
